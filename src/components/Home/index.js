// @flow
import React from "react";
import { CardListContainer, FormContainer } from "containers/Users";

const Home = () => (
  <div className="container">
    <FormContainer />
    <CardListContainer />
  </div>
);

export default Home;
